{
  "name": "tino",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm --filter @tino/web dev",
    "build": "pnpm --filter @tino/web build",
    "preview": "pnpm --filter @tino/web preview",
    "lint": "eslint ."
  },
  "devDependencies": {
    "@eslint/js": "^10.0.0",
    "eslint": "^10.0.0",
    "eslint-plugin-react-hooks": "^7.0.1",
    "eslint-plugin-react-refresh": "^0.4.0",
    "globals": "^16.0.0",
    "typescript": "^5.9.0",
    "typescript-eslint": "^8.0.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild"
    ]
  }
}
