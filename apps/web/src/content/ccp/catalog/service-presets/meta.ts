import type { DocumentMeta } from "@entities/document";

const meta: DocumentMeta = {
  title: "Service Presets",
  description: "GitOps 기반 사전 구성된 파이프라인/트리거 프리셋 카탈로그",
  createdAt: "2026-03-10",
  tags: ["ccp", "catalog", "service-presets", "pipeline", "trigger"],
};

export default meta;
